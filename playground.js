// import React from "react";
import {dialogues} from './src/components/DialogueBox/dialogue.js'

function test(arr) {
    for (let each of arr) {
        return each
    }
    if (err) { console.log(err)}
}
test(dialogues)